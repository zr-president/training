// AI Agent 实操 · 内容与配置（对应字节 JD 第 2 条硬性要求：
// "对 AI 产品/LLM 应用/Agent/Workflow 有较强兴趣，对 AI 如何提升运营效率有一定理解与思考"）
// 本模块不是"看概念"，而是：判读设计 → 算清提效 → 动手做 → 沉淀模板

var AGENT_CONTENT = {
  updated: '2026-09-12',

  intro: 'JD 要求的不是"知道 Agent 是什么"，而是"理解 AI 如何提升运营效率"。后者只能靠**动手做过 + 算得清账**来证明。本模块包含四部分：①设计判读 ②提效计算器 ③实操任务清单 ④可复用模板（可直接拿去 Harness 跑）。',

  /* ---------- 一、设计判读（多选，自动判分） ---------- */
  suitability: {
    id: 'ag_suit',
    title: '哪些运营任务值得做成 Agent？',
    scenario: '你手里有 8 类日常运营任务。搭 Agent 是有成本的（开发 + 维护 + 审核），所以第一件事是判断"哪些值得做"。',
    ask: '选出**真正适合**做成 Agent 的任务（多选）。判断标准：频次高 × 规则相对清晰 × 容错空间可接受 × 人工审核成本低。',
    options: [
      { t: '每日竞品动态汇总（固定几个信息源，抓取→摘要→结构化输出）', ok: true,
        note: '✅ 高频、信息源固定、格式模板化、错了也不致命（人工扫一眼即可）。这是最典型的 Agent 场景。' },
      { t: '周报数据整理（从固定表拉数→按口径算指标→生成初稿）', ok: true,
        note: '✅ 高频 + 规则清晰（口径固定）。Agent 做初稿，人做判断与结论——这是"人机分工"的最佳形态。' },
      { t: '异常指标预警（监控核心指标，超阈值时自动生成排查线索）', ok: true,
        note: '✅ 高频巡检 + 规则明确。Agent 负责"发现异常并给出排查方向"，人负责决策——大幅缩短发现到响应的时间。' },
      { t: '用户投诉初筛分类（自动打标签 → 分派给对应团队）', ok: true,
        note: '✅ 高频、分类规则清晰、有明确的人工复核环节。分类错误的成本可通过抽检控制。' },
      { t: '给老板做重大策略决策（决定明年预算怎么分配）', ok: false,
        note: '❌ 低频 + 高影响 + 高度依赖上下文判断。这类事需要人负责，Agent 只能提供分析素材，不能拍板——"AI 结论必须人工核验"。' },
      { t: '一次性的活动方案策划（只做一次，且没有历史数据）', ok: false,
        note: '❌ 低频且一次性——搭建与调试成本收不回来。一次性任务直接让 LLM 生成即可，不需要 Agent（Agent 的价值在"重复执行的自动化"）。' },
      { t: '需要实时人际沟通的商务谈判辅助', ok: false,
        note: '❌ 强交互、强语境、后果重大，且 Agent 无法对沟通结果负责。' },
      { t: '把公司核心用户数据上传到第三方 Agent 处理', ok: false,
        note: '❌ 数据合规红线。涉及用户隐私/核心数据的场景必须走本地部署（如开源的 DeepSeek V4.1 Flash / Qwen 可私有化）或内部审批，不能随手用公有云服务。' }
    ],
    keyPoints: [
      '适合：高频 × 规则清晰 × 容错可接受 × 审核成本低',
      '不适合：低频一次性 / 高影响决策 / 强人际交互 / 数据合规红线',
      'Agent 的价值来自「重复执行的自动化」——一次性任务用普通 LLM 就够',
      '红线：涉及用户隐私与核心数据，必须本地部署或走内部审批'
    ],
    takeaway: '判断一个任务该不该做 Agent，问四句：**多久做一次？规则清不清楚？错了后果大不大？人要花多少时间审核？**'
  },

  /* ---------- 二、设计判读（单选，多题，自动判分） ---------- */
  designs: [
    {
      id: 'ag_d1', tag: 'Agent 设计',
      title: '「自动周报 Agent」的流程该怎么设计？',
      scenario: '你要搭一个"自动生成运营周报"的 Agent。数据在内部数据库，周报需要 6 个指标 + 一段趋势解读，最终要发给主管。',
      ask: '下面哪种设计最合理？',
      options: [
        { t: '让 LLM 直接"凭理解"写出 6 个指标和解读，一步生成完整周报', ok: false,
          note: '❌ 致命问题：LLM 不会算数。指标必须来自**查数**（SQL/API），不能让模型编。这是 Agent 设计里最常见的错误——把"取数"也交给模型。' },
        { t: '拆成多步：①用 SQL 从数据库取数（工具调用）②把数据交给模型做趋势解读 ③组装成周报草稿 ④人工审核后发送', ok: true,
          note: '✅ 正确设计。关键点：**取数与生成分离**（数字由代码算、解读由模型写），并在最后保留人工审核环节。这也是"人机分工"的标准形态。' },
        { t: '让 Agent 每 5 分钟生成一次周报，保证数据最新', ok: false,
          note: '❌ 频次与需求不匹配。周报是周频需求，高频生成只会浪费算力并制造审核负担。Agent 的调度频率应匹配业务频率。' },
        { t: '去掉人工审核，直接自动发给主管，节省时间', ok: false,
          note: '❌ 高风险设计。少了审核，一旦取数口径错或模型幻觉，错误会直接对外发出——对主管发错数据的代价远大于省下的审核时间。' }
      ],
      keyPoints: ['取数与生成必须分离：数字用代码算，解读用模型写', '保留人工审核环节（尤其对外输出）', '调度频率要匹配业务频率'],
      takeaway: 'LLM 不会算数。**凡是"数字"，都必须由代码从数据源算出**，模型只负责解读与组织语言。'
    },
    {
      id: 'ag_d2', tag: '失败处理',
      title: 'Agent 取数失败时该怎么办？',
      scenario: '你的周报 Agent 每天 9 点跑，依赖从内部数据库取数。某天数据库维护，连接失败。',
      ask: '最合理的设计是？',
      options: [
        { t: '失败就重试 3 次（间隔递增），仍失败则记录日志+推送告警给人，并跳过本次执行而不是发一份空周报', ok: true,
          note: '✅ 正确。要点：①有限重试（指数退避）②失败要**告警到人**而不是静默 ③**宁可不发，也不发错的**——空数据周报比没周报危害更大。' },
        { t: '无限重试，直到成功为止', ok: false,
          note: '❌ 会打爆数据库连接、产生大量日志，且可能造成雪崩。必须设重试上限与退避策略。' },
        { t: '捕获异常后继续执行，用上次的数据凑一份', ok: false,
          note: '❌ 用旧数据冒充新数据是数据造假，会误导决策。这种情况必须明确标注或直接中止。' },
        { t: '静默失败，不打扰任何人，下次跑成功就好', ok: false,
          note: '❌ 静默失败最危险——你以为在正常运行，实际已经连续失败一周。Agent 必须有"沉默即异常"的告警机制。' }
      ],
      keyPoints: ['有限重试 + 指数退避', '失败必须告警到人，不能静默', '宁可不发也不能发错数据'],
      takeaway: '自动化系统的可靠性不在于"从不失败"，而在于**失败时能被发现、且不会输出错误结果**。'
    },
    {
      id: 'ag_d3', tag: '成本与选型',
      title: '这个 Agent 该用哪个模型？',
      scenario: '你的 Agent 每天要处理约 200 条竞品信息（每条 500-1500 字），任务是摘要 + 分类打标。要求：成本可控、可处理长文本、分类准确率够用。涉及的数据是公开信息。',
      ask: '怎么选型最合理？',
      options: [
        { t: '全部用最贵的旗舰模型（如 GPT-6 Astra，$10/$50 每百万 token）', ok: false,
          note: '❌ 任务难度与模型能力不匹配：摘要+分类属于中等难度，用旗舰模型是"高射炮打蚊子"。这类高频批处理任务，成本会被放大 200 倍/天。' },
        { t: '用开源/低价模型（如 DeepSeek V4.1 Flash，峰值 $0.3/$1.2，本地部署免费）先跑通，实测准确率不够再升级', ok: true,
          note: '✅ 正确思路：**先低成本跑通，用数据决定要不要升级**。摘要+分类这类任务，中等模型通常已足够；而且开源模型可本地部署，长期成本更低。' },
        { t: '用最小的模型，省到极致，准确率不重要', ok: false,
          note: '❌ 走另一个极端。分类错误的成本（人工返工）可能远高于模型差价——要算的是"总成本"，不是"token 单价"。' },
        { t: '把所有信息先人工筛一遍，再交给模型处理', ok: false,
          note: '❌ 这就失去了自动化的意义。人工初筛等于把最耗时的部分留给了人。' }
      ],
      keyPoints: ['模型选型要匹配任务难度，不是越贵越好', '高频批处理任务对成本最敏感', '算"总成本"（含返工）而不是"token 单价"'],
      takeaway: '选型原则：**先便宜模型跑通，用实测准确率决定是否升级**。省下的钱是利润，多花的钱要能换来对应的准确率。'
    }
  ],

  /* ---------- 三、提效计算器 ---------- */
  calc: {
    title: '提效计算器（把账算清楚）',
    warning: '最常见的错误算法：只算「人工耗时 − Agent 生成耗时」。真实的账必须加上**人工审核**和**维护调试**时间——很多 Agent 项目就是在这两项上把自己算亏的。',
    presets: [
      { name: '运营周报', freq: 1, manual: 120, agent: 3, review: 15, upkeep: 20, rate: 80 },
      { name: '每日竞品汇总', freq: 5, manual: 40, agent: 2, review: 6, upkeep: 15, rate: 80 },
      { name: '用户反馈分类', freq: 5, manual: 60, agent: 2, review: 10, upkeep: 10, rate: 80 },
      { name: '指标异常巡检', freq: 7, manual: 15, agent: 1, review: 3, upkeep: 8, rate: 80 }
    ],
    fields: [
      { k: 'freq', label: '每周执行次数', unit: '次/周' },
      { k: 'manual', label: '纯人工单次耗时', unit: '分钟' },
      { k: 'agent', label: 'Agent 单次生成耗时', unit: '分钟' },
      { k: 'review', label: '人工审核单次耗时', unit: '分钟' },
      { k: 'upkeep', label: '维护调试（每周合计）', unit: '分钟/周' },
      { k: 'rate', label: '你的时薪', unit: '元/小时' }
    ]
  },

  /* ---------- 四、实操任务清单 ---------- */
  tasks: [
    { id: 't1', title: '跑通第一个自动化脚本', goal: '用 Python 把"每日资讯汇总"跑通：抓取 3 个信息源 → 生成结构化摘要 → 输出到本地文件。',
      standard: '连续 3 天能无人值守跑出结果文件；失败时有日志可查。',
      tips: '先别追求完美。第一步的价值是"跑通链路"，不是"做得好看"。' },
    { id: 't2', title: '接上模型：做一个周报生成器', goal: '用 Harness + DeepSeek V4.1 Flash 把「取数 → 解读 → 成文」串起来，产出周报初稿。',
      standard: '能生成一份包含 6 个指标 + 一段趋势解读的周报，且数字全部来自真实查询（不是模型编的）。',
      tips: '关键纪律：数字用代码算，模型只写解读。这一步做对了，后面都不会错。' },
    { id: 't3', title: '加失败重试 + 人工审核环节', goal: '给 Agent 加上：有限重试（3 次 / 指数退避）、失败告警、生成结果先落草稿不直接外发。',
      standard: '手动断开数据源测试：能正确重试、失败后告警、不输出错误内容。',
      tips: '这是"能演示的玩具"和"能上生产的工具"之间的分界线。' },
    { id: 't4', title: '量化提效并写成作品', goal: '用提效计算器算出真实的节省时间与年化价值，写成 300 字案例（含改进前后对比）。',
      standard: '案例里有具体数字：每周节省 X 小时、年化价值 Y 元、审核与维护成本已计入。',
      tips: '面试时这段最有说服力——它证明你"真的做过"，而不是"了解 Agent"。' }
  ],

  /* ---------- 五、可复用模板 ---------- */
  templates: [
    {
      name: '周报生成 Agent · Prompt 模板',
      desc: '把 {{}} 里的内容替换成你的实际字段即可。要点：明确角色、输入数据格式、输出结构、并禁止模型编造数字。',
      lang: 'text',
      body: `你是{{公司/团队}}的运营分析助手。你的任务是把已计算好的数据，组织成一份结构清晰的运营周报。

## 硬性约束（必须遵守）
1. 我只能提供已经算好的数据。你**绝对不得**自行推算、猜测或编造任何数字。
2. 如果某个数据缺失，直接写「数据缺失」，不要用估算值填充。
3. 所有结论必须能在给定数据中找到依据；不要引入数据之外的信息。

## 输入数据（JSON）
{{在这里粘贴指标 JSON，例如：
{"week":"2026-W37","dau":12345,"dau_wow":-2.1,"new_users":890,"d1_rate":31.2,"pay_rate":2.8,"gmv":156000}
}}

## 输出结构（严格按此顺序）
1. **本周概览**：3 句话内说清整体表现（涨/跌 + 关键数字）
2. **指标明细**：用表格列出所有指标 + 环比
3. **异常与可能原因**：只针对波动超过 ±5% 的指标，每条给 1-2 个**可验证的**原因假设
4. **下周建议**：2-3 条具体动作，每条要有明确的责任方向

## 语气
客观、简洁、结论先行。不要用"取得了显著成绩"这类空话。`
    },
    {
      name: '每日竞品汇总 Agent · 工作流骨架（Python）',
      desc: '可直接改造的骨架：抓取 → 清洗 → 模型摘要 → 落盘 → 失败告警。核心是"有限重试 + 不静默失败"。',
      lang: 'python',
      body: `# 每日竞品动态汇总 · 最小可用骨架
# 依赖: pip install requests
# 关键纪律：①有限重试 ②失败告警不静默 ③结果落盘可追溯

import json, time, datetime, pathlib, requests

SOURCES = [
    {"name": "example-blog", "url": "https://example.com/feed.json"},
]
OUT_DIR = pathlib.Path("out"); OUT_DIR.mkdir(exist_ok=True)

def fetch(url, retries=3):
    """有限重试 + 指数退避：不无限重试（会打爆对方/自己）"""
    for i in range(retries):
        try:
            r = requests.get(url, timeout=15)
            r.raise_for_status()
            return r.text
        except Exception as e:
            wait = 2 ** i
            print(f"[warn] 第{i+1}次失败: {e}，{wait}s 后重试")
            time.sleep(wait)
    raise RuntimeError(f"取数失败（已重试{retries}次）: {url}")

def summarize(items):
    """这里调用模型（Harness / DeepSeek V4.1 Flash）
       重要：只让模型做摘要与分类，不要让模型编造数字"""
    # TODO: 替换为你自己的模型调用
    return [{"title": it.get("title", ""), "tag": "待分类"} for it in items]

def main():
    today = datetime.date.today().isoformat()
    all_items = []
    for s in SOURCES:
        try:
            raw = fetch(s["url"])
            all_items += json.loads(raw)
            print(f"[ok] {s['name']}: {len(all_items)} 条")
        except Exception as e:
            # 关键：失败必须告警到人，不能静默
            print(f"[ALERT] 信息源 {s['name']} 抓取失败: {e}")

    if not all_items:
        # 宁可不发，也不发空结果
        raise SystemExit("[ABORT] 所有信息源都失败，终止本次执行（不发空汇总）")

    result = summarize(all_items)
    out = OUT_DIR / f"digest-{today}.json"
    out.write_text(json.dumps(result, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"[done] 已输出 {out}（{len(result)} 条）")

if __name__ == "__main__":
    main()`
    },
    {
      name: '提效核算表 · 模板（写进作品集用）',
      desc: '把自己的 Agent 项目按这个结构写成案例——面试时比"我了解 Agent"强 10 倍。',
      lang: 'text',
      body: `【项目】运营周报自动生成 Agent
【背景】原来每周一人工拉数+写周报，耗时 2 小时，且口径偶尔不一致
【方案】
  ①数据层：SQL 固定口径取数（6 个核心指标）
  ②生成层：模型负责趋势解读与成文（禁止编造数字）
  ③审核层：生成草稿 → 人工审 15 分钟 → 确认后发送
  ④可靠性：有限重试 + 失败告警 + 绝不发空数据
【量化效果】
  · 人工耗时：120 分钟 → 18 分钟（生成 3 分钟 + 审核 15 分钟）
  · 每周节省：1.7 小时；年化约 88 小时
  · 口径一致性：从"依赖人"变为"SQL 固定口径"，消除口径漂移
【踩过的坑】
  · 最初让模型直接写指标 → 出现编造数字，改为"数字必须来自查询"
  · 最初失败时静默重试 → 连续 5 天没发出周报无人发现，加了告警
【可复用点】取数与生成分离 + 人工审核卡点 + 失败告警——这三条适用于所有运营自动化`
    }
  ],

  resources: [
    { t: 'Harness（你的主力 Agent 框架）', d: '配合 DeepSeek V4.1 Flash / V4-Pro 使用；本地部署可用 Qwen3.8-27B', u: '#' },
    { t: 'DeepSeek V4.1 Flash（9/10 发布）', d: 'MIT 开源、原生视觉、API 峰值 $0.3/$1.2——高频批处理任务的最优性价比', u: 'https://huggingface.co/deepseek-ai' },
    { t: '你的个人网站（信息中枢）', d: '信息聚合与每日更新的自动化实践，本身就是 Agent 案例', u: 'https://zr-president.github.io/my-website/' }
  ]
};
