# -*- coding: utf-8 -*-
"""扩充题库：lab +4 / metrics +2 / abtest +3 / cases +2"""
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
BASE = r'C:\Users\ZR\Desktop\钟锐的训练场\data'

def append(fp, new_items, label):
    c = open(fp, 'r', encoding='utf-8').read()
    i = c.rfind("\n];")
    if i < 0:
        print('MISS end:', label); return
    c = c[:i] + ",\n\n" + new_items + "\n];" + c[i + 3:]
    open(fp, 'w', encoding='utf-8').write(c)
    print('OK', label)

# ============ lab +4 ============
LAB_NEW = r'''
{
  id:'lab09', icon:'🚀', tag:'新用户激活',
  title:'新用户的"首单之路"卡在哪一步？',
  ctx:'新用户从注册到第一次付费要经过好几步。老板问：我们到底卡在哪一步？',
  deliverable:'画出新用户激活漏斗（注册→登录→发帖/互动→付费），指出最大的流失环节，并给出 2 条改进动作。',
  steps:[
    '第一步：先看"注册但从未登录"的用户有多少（最前端就漏了）',
    '第二步：看登录过但从未产生互动（发帖/评论）的用户占比',
    '第三步：看互动过的用户里有多少付费',
    '第四步：算出每一步的转化率，找出流失最大的那一环',
    '第五步：针对最大流失环给动作'
  ],
  queries:[
    { t:'① 注册但从未登录（最前端流失）',
      sql:`SELECT COUNT(*) AS never_login_users
FROM users u
WHERE NOT EXISTS (SELECT 1 FROM events e
                  WHERE e.user_id=u.user_id AND e.event_type='login')`,
      finding:'这批人连产品都没进来过——是注册流程、渠道质量或手机号验证的问题，跟产品体验无关。' },
    { t:'② 四级激活漏斗',
      sql:`SELECT
  (SELECT COUNT(*) FROM users) AS s1_注册,
  (SELECT COUNT(DISTINCT user_id) FROM events WHERE event_type='login') AS s2_登录,
  (SELECT COUNT(DISTINCT user_id) FROM events
     WHERE event_type IN ('post','comment','share')) AS s3_互动,
  (SELECT COUNT(DISTINCT user_id) FROM orders) AS s4_付费`,
      finding:'四个数字横向一比，最大的断层就是你要优化的地方。注意"互动"用 IN 把三类行为合并（任一即算）。' },
    { t:'③ 各步骤转化率（更直观）',
      sql:`WITH f AS (
  SELECT
    (SELECT COUNT(*) FROM users) AS s1,
    (SELECT COUNT(DISTINCT user_id) FROM events WHERE event_type='login') AS s2,
    (SELECT COUNT(DISTINCT user_id) FROM events
       WHERE event_type IN ('post','comment','share')) AS s3,
    (SELECT COUNT(DISTINCT user_id) FROM orders) AS s4
)
SELECT s1, s2, ROUND(s2*100.0/s1,1) AS 注册到登录,
       s3, ROUND(s3*100.0/s2,1) AS 登录到互动,
       s4, ROUND(s4*100.0/s3,1) AS 互动到付费
FROM f`,
      finding:'"注册→登录"和"互动→付费"通常是流失最大的两环，但原因完全不同：前者是渠道/流程问题，后者是价值感知问题。' }
  ],
  conclusion:'漏斗分析的关键不是算出一堆百分比，而是**指出断层最大的一环并解释原因**。经验上：注册→登录的断层通常来自渠道质量或验证流程摩擦；登录→互动的断层来自"没有内容可看/没有引导"；互动→付费的断层来自付费理由不足。**给动作时要对应到具体那一环**，不要笼统说"优化体验"。'
},
{
  id:'lab10', icon:'🏆', tag:'渠道决策',
  title:'综合评估：哪个渠道最健康？',
  context:'前面几题分别看过量、留存、付费、单位经济。现在要综合成一个结论。',
  ctx:'前面几题分别看过量、留存、付费、单位经济。现在要综合成一个结论。',
  question:'如果只能保留 3 个渠道，你留哪 3 个？给出排名依据（要求同时覆盖规模、质量、成本三个维度）。',
  deliverable:'一个排名表（含三维指标 + 综合排序）+ 一句话结论 + 被淘汰渠道的处理建议。',
  steps:[
    '第一步：把三维指标放在一张表里（用户数 / 次日留存 / LTV-CAC）',
    '第二步：不要简单相加——先判断哪个维度是当前瓶颈（规模优先还是效率优先）',
    '第三步：给出你的排序逻辑（并说明为什么这样排）',
    '第四步：对被淘汰渠道给处理建议（直接停 / 优化后再看）'
  ],
  queries:[
    { t:'① 三维综合表',
      sql:`SELECT u.channel,
  COUNT(DISTINCT u.user_id) AS users,
  ROUND(COUNT(DISTINCT CASE WHEN e.event_type='login'
        AND e.event_date=date(u.register_date,'+1 day')
        THEN u.user_id END)*100.0/COUNT(DISTINCT u.user_id),2) AS d1_rate,
  ROUND(SUM(o.amount)*1.0/COUNT(DISTINCT u.user_id),2) AS ltv,
  ROUND(COALESCE(c.cost,0)*1.0/COUNT(DISTINCT u.user_id),2) AS cac,
  ROUND((SUM(o.amount)*1.0/COUNT(DISTINCT u.user_id))
        / NULLIF(COALESCE(c.cost,0)*1.0/COUNT(DISTINCT u.user_id),0),2) AS ltv_cac
FROM users u
LEFT JOIN events e ON u.user_id=e.user_id
LEFT JOIN orders o ON o.user_id=u.user_id
LEFT JOIN channels c ON c.channel=u.channel
WHERE u.register_date < '2026-09-10'
GROUP BY u.channel
ORDER BY users DESC`,
      finding:'NULLIF(...,0) 用来避免自然流量渠道 cost=0 时除零报错——返回 NULL 表示"无成本，比值无意义"，比报错好。' },
    { t:'② 各渠道对总收入的贡献占比',
      sql:`SELECT u.channel,
  ROUND(COALESCE(SUM(o.amount),0),2) AS revenue,
  ROUND(COALESCE(SUM(o.amount),0)*100.0
        /(SELECT SUM(amount) FROM orders),2) AS rev_share
FROM users u LEFT JOIN orders o ON o.user_id=u.user_id
GROUP BY u.channel
ORDER BY revenue DESC`,
      finding:'收入贡献占比会揭示"哪些渠道虽然量小但赚钱多"——这类渠道往往被忽视。' }
  ],
  conclusion:'综合决策没有唯一正确答案，但**必须说清你的排序依据**。专业答法示例："当前阶段规模已不是瓶颈，因此以 LTV/CAC 为第一排序标准，规模作为次要标准"——先声明判断标准，再给结论。**最忌讳把三个指标简单相加**（量纲不同、重要性不同）。另外要区分：自然流量渠道（朋友推荐）的特殊价值不在 ROI 而在"零成本 + 高留存"，属于必须保护的战略资产。'
},
{
  id:'lab11', icon:'⚠️', tag:'流失预警',
  title:'定义"即将流失"的用户并评估规模',
  ctx:'运营想在用户流失前就介入，而不是等流失后再召回。但"即将流失"必须能被定义和度量。',
  question:'你如何定义"即将流失"？按你的定义圈出这批人，并给出规模与渠道分布。',
  deliverable:'流失定义（含阈值与理由）+ 可触达规模 + 渠道分布 + 一条预警规则。',
  steps:[
    '第一步：先算"活跃用户"的典型使用间隔（判断多长不来算异常）',
    '第二步：把"曾经活跃但最近 N 天未登录"定义为高危',
    '第三步：算规模，并按渠道拆分',
    '第四步：把定义写成一条可自动执行的预警规则'
  ],
  queries:[
    { t:'① 活跃用户的平均使用间隔（用于定阈值）',
      sql:`SELECT ROUND(AVG(gap),2) AS avg_gap_days
FROM (
  SELECT user_id, event_date,
    julianday(event_date) - julianday(LAG(event_date) OVER (PARTITION BY user_id ORDER BY event_date)) AS gap
  FROM events WHERE event_type='login'
) t WHERE gap IS NOT NULL`,
      finding:'用 LAG 窗口函数算"上一次登录到这一次登录"的间隔，平均间隔的 2-3 倍就是合理的"沉默"阈值——这比拍脑袋定"7 天"更有依据。' },
    { t:'② 高危用户规模（曾活跃 ≥3 天、但最近 10 天未登录）',
      sql:`SELECT COUNT(*) AS at_risk_users
FROM (
  SELECT u.user_id
  FROM users u JOIN events e ON u.user_id=e.user_id AND e.event_type='login'
  GROUP BY u.user_id
  HAVING COUNT(DISTINCT e.event_date) >= 3
     AND MAX(e.event_date) <= '2026-08-31'
) t`,
      finding:'同时限定"历史活跃度"和"最近未活跃"——只满足一个条件都不算高危。' },
    { t:'③ 高危用户的渠道分布',
      sql:`SELECT u.channel, COUNT(*) AS at_risk
FROM (
  SELECT uu.user_id, uu.channel
  FROM users uu JOIN events e ON uu.user_id=e.user_id AND e.event_type='login'
  GROUP BY uu.user_id, uu.channel
  HAVING COUNT(DISTINCT e.event_date) >= 3
     AND MAX(e.event_date) <= '2026-08-31'
) u
GROUP BY u.channel
ORDER BY at_risk DESC`,
      finding:'渠道集中度高的高危人群，说明某个渠道的用户体验或预期出现了系统性问题。' }
  ],
  conclusion:'流失预警的核心是**把"感觉要流失"变成可执行的规则**：①阈值要有依据（用平均使用间隔推导，而不是拍脑袋）②要同时看历史活跃度和最近行为（只满足一个都不算）③规则要能自动跑（写成 SQL 定时执行）。**预警的价值在于"提前介入"**——用户流失后再召回，成本高得多、成功率低得多。'
},
{
  id:'lab12', icon:'✍️', tag:'内容供给',
  title:'谁在产出内容？发帖用户的特征',
  ctx:'内容平台最怕"看的人多、发的人少"。要先弄清楚：产出内容的是怎样一群人？',
  question:'描述发帖用户的画像（渠道/城市/年龄/活跃度），并估算他们占全站的比例。',
  deliverable:'发帖用户画像 + 占比 + 一条"如何扩大供给"的建议。',
  steps:[
    '第一步：圈出发过帖的用户（去重）',
    '第二步：算他们占全站用户的比例（供给率）',
    '第三步：看他们的渠道/城市/年龄分布，与全站对比',
    '第四步：看他们的活跃度是否显著高于非发帖用户',
    '第五步：基于画像给扩供给的建议'
  ],
  queries:[
    { t:'① 发帖用户占比（供给率）',
      sql:`SELECT
  (SELECT COUNT(DISTINCT user_id) FROM events WHERE event_type='post') AS posters,
  (SELECT COUNT(*) FROM users) AS total_users,
  ROUND((SELECT COUNT(DISTINCT user_id) FROM events WHERE event_type='post')*100.0
        /(SELECT COUNT(*) FROM users),2) AS poster_rate`,
      finding:'供给率是内容平台的核心健康指标。业内通常不到 5% 的用户会主动产出——这个比例决定内容的丰富度。' },
    { t:'② 发帖用户的渠道/年龄画像',
      sql:`SELECT u.channel, COUNT(DISTINCT u.user_id) AS posters, ROUND(AVG(u.age),1) AS avg_age
FROM users u
WHERE EXISTS (SELECT 1 FROM events e WHERE e.user_id=u.user_id AND e.event_type='post')
GROUP BY u.channel
ORDER BY posters DESC`,
      finding:'把发帖用户的渠道分布与全站渠道分布对比，就能看出"哪个渠道更容易产出内容"——这是投放结构的优化依据。' },
    { t:'③ 发帖用户 vs 非发帖用户的活跃度差异',
      sql:`SELECT CASE WHEN p.user_id IS NULL THEN '非发帖用户' ELSE '发帖用户' END AS grp,
  COUNT(*) AS users,
  ROUND(AVG(a.d),2) AS avg_active_days
FROM (SELECT user_id, COUNT(DISTINCT event_date) AS d
      FROM events WHERE event_type='login' GROUP BY user_id) a
LEFT JOIN (SELECT DISTINCT user_id FROM events WHERE event_type='post') p ON p.user_id=a.user_id
GROUP BY grp`,
      finding:'发帖用户的活跃天数通常是普通用户的数倍——这说明"创作"和"留存"是强绑定的，扩供给本身就是在做留存。' }
  ],
  conclusion:'供给分析的结论要落到**"如何扩大供给"**：①供给率低说明绝大多数人只消费——要降低创作门槛（模板/话题引导）；②如果某渠道用户特别爱发帖，就加大该渠道投放；③"发帖用户活跃度显著更高"意味着**引导创作本身就是留存手段**，而不是单纯的内容运营动作。'
}
'''

# ============ metrics +2 ============
METRICS_NEW = r'''
{
  id:'m07', tag:'本地生活', icon:'🏪',
  scenario:'你在本地生活（到店）业务做策略运营，要搭一套「门店经营健康度」指标体系，用来判断哪些门店值得重点扶持、哪些该优化。',
  ask:'选出应该纳入的指标（多选）。',
  options:[
    { t:'门店月 GMV 与环比增速', ok:true, note:'✅ 规模与趋势。只看绝对值看不出好坏（大店天然高），必须配合增速。' },
    { t:'核销率（购买券后实际到店核销的比例）', ok:true, note:'✅ 本地生活最关键的指标。卖出去不等于消费掉——核销率低说明商家履约能力或商品描述有问题，还埋着退款风险。' },
    { t:'客单价与团购套餐价格带分布', ok:true, note:'✅ 价格带分布能看出门店是"低价引流"还是"正常经营"，对判断可持续性很重要。' },
    { t:'用户评价分与差评率', ok:true, note:'✅ 体验质量的代理指标，且直接影响后续流量转化。' },
    { t:'复购率（同一用户二次到店比例）', ok:true, note:'✅ 本地生活的核心价值就在复购——一次性的薅羊毛用户不构成健康经营。' },
    { t:'门店曝光量（列表页展示次数）', ok:false, note:'❌ 这是平台分配的结果指标，不是门店经营质量。曝光高可能是因为投了钱，不代表门店健康。' },
    { t:'该门店所属品类的平台总 GMV', ok:false, note:'❌ 品类大盘指标，不能用来评估单个门店，属于粒度错配。' },
    { t:'门店在 App 内的页面停留时长', ok:false, note:'❌ 弱相关且易被误读（停留久可能是纠结不下单）。衡量意向应该看"下单转化率"这类更接近结果的行为。' }
  ],
  north:'北极星指标：**门店月度有效 GMV**（已核销金额）——用"核销"而不是"卖出"，避免把退款和未履约算成业绩。',
  tree:[
    '规模：有效 GMV / 订单量 / 环比增速',
    '转化：曝光→点击→下单转化率 / 核销率',
    '用户：复购率 / 新客占比 / 客单价',
    '质量：评价分 / 差评率 / 退款率'
  ],
  pitfalls:[
    '用"卖出 GMV"代替"核销 GMV"——会高估业绩并掩盖履约问题',
    '把平台分配的曝光量当门店质量——粒度与因果都错',
    '只看 GMV 不看复购——一次性生意无法持续'
  ],
  takeaway:'本地生活类业务的命门是**核销**：卖出去只是开始，消费掉才算收入。指标体系里没有核销率，就等于对最大的风险点视而不见。'
},
{
  id:'m08', tag:'推荐效果', icon:'🎯',
  scenario:'你负责评估推荐系统的效果，要回答一个问题：新的推荐策略到底有没有让人更愿意用？',
  ask:'选出应该纳入的指标（多选）。',
  options:[
    { t:'人均消费内容数 / 完播率（消费深度）', ok:true, note:'✅ 推荐效果的直接影响面——推荐得准，用户看得更多更久。' },
    { t:'次日/7日留存率', ok:true, note:'✅ 推荐的终极检验：能不能让人明天还来。只看当次时长容易被"标题党"式推荐骗过去。' },
    { t:'多样性指标（用户消费内容的品类分布熵）', ok:true, note:'✅ 防止推荐把人锁进"信息茧房"。短期时长涨、多样性暴跌，长期会伤害留存——这是推荐系统最典型的长期代价。' },
    { t:'冷启动内容的曝光占比（新内容/新作者获得曝光的比例）', ok:true, note:'✅ 供给侧健康度。推荐如果只推头部，会掐死新内容供给，最终损害生态。' },
    { t:'点不感兴趣 / 举报 / 屏蔽率', ok:true, note:'✅ 负反馈是重要的纠偏信号。只看正反馈（点击）会系统性高估效果。' },
    { t:'模型 AUC / 离线评测指标', ok:false, note:'❌ 离线指标不能替代线上效果——AUC 提升但线上时长下降的案例很多。离线指标属于研发侧参考，不是业务健康度指标。' },
    { t:'推荐接口的响应耗时（P99）', ok:false, note:'❌ 技术性能指标，属于工程 SLA，不是业务效果指标（虽然重要，但不该混进业务指标体系）。' },
    { t:'推荐策略的代码提交次数', ok:false, note:'❌ 过程/工作量指标，与效果无因果关系。典型的活动量指标。' }
  ],
  north:'北极星指标：**留存加权的内容消费深度**——把"看得多"和"明天还来"绑在一起，避免为了短期时长牺牲长期留存。',
  tree:[
    '消费深度：人均消费数 / 完播率 / 人均时长',
    '留存：次日留存 / 7 日留存',
    '生态健康：内容多样性 / 冷启动曝光占比 / 头部集中度',
    '负反馈：点不感兴趣率 / 举报率 / 屏蔽率'
  ],
  pitfalls:[
    '只看时长不看留存——标题党式推荐能短期拉时长，长期杀留存',
    '忽略多样性与冷启动——把流量全给头部，短期指标好看，生态枯竭',
    '用离线指标（AUC）替代线上效果——两者经常不一致'
  ],
  takeaway:'评估推荐系统要同时看**正反馈（点击/时长）和负反馈（举报/屏蔽）+ 长期（留存）+ 生态（多样性）**。只看其中一个维度，一定会把系统带偏。'
}
'''

# ============ abtest +3 ============
ABTEST_NEW = r'''
{
  id:'a07', tag:'分流质量', icon:'⚖️',
  scenario:'实验上线后发现：按计划实验组与对照组应该各 5,000 人，但实际实验组只有 4,000 人、对照组 6,000 人（分流比例 40:60，而设计是 50:50）。指标结果是实验组 +3%、p=0.04。',
  ask:'你该怎么处理这个实验结果？',
  options:[
    { t:'p<0.05，结果可用，直接上线', ok:false,
      note:'❌ 分流比例严重偏离设计，说明分流逻辑（哈希/随机数/用户标识）可能有问题——这种情况下两组人群本身可能不可比，p 值再小也不能信。' },
    { t:'先排查分流不均的原因（分流算法/埋点/实验配置），修复后重跑实验，本次结果不作为决策依据', ok:true,
      note:'✅ 正确。分流不均是实验系统的红线问题。而且"实验组样本少"本身可能就与用户属性相关（比如某些用户根本没进实验），会让两组不可比。' },
    { t:'用统计方法校正（如按比例重算），继续得出结论', ok:false,
      note:'❌ 比例可以校正，但"为什么少"的原因无法用数学补上——如果是系统性问题（如部分用户进不了实验组），校正毫无意义。' },
    { t:'把对照组随机砍掉 2,000 人凑成 1:1', ok:false,
      note:'❌ 事后人为裁剪样本会引入选择偏差，而且丢失了"分流为何不均"这个更重要的信号。' }
  ],
  keyPoints:[
    '分流比例偏离设计 = 实验系统可能有 bug',
    '样本量偏差往往与用户属性相关 → 两组不可比',
    '先修系统，再重跑；本次结果不作为决策依据'
  ],
  takeaway:'实验的前提是**两组可比**。分流不均不是"小瑕疵"，它直接摧毁了实验的因果解释力——修好再做，不要用统计手段去圆一个坏实验。'
},
{
  id:'a08', tag:'指标冲突', icon:'🔀',
  scenario:'实验：新版首页把"关注流"提到第一位。结果：\n· 人均使用时长 +6%（p=0.008，显著）\n· 新增关注数 −9%（p=0.01，显著）\n· 7 日留存 +0.1%（p=0.62，不显著）',
  ask:'你怎么解读这组结果并给出建议？',
  options:[
    { t:'时长显著提升，直接全量上线', ok:false,
      note:'❌ 时长涨是"被动消费"变多，但关注数下降意味着用户主动建立的关系变少——这正是长期留存的燃料。只看时长会漏掉这个隐患。' },
    { t:'时长涨、关注跌，是典型的"短期指标改善、长期机制受损"信号；建议延长观察期看留存是否转负，或改为分层设计（新用户用新版、老用户保留关注流）', ok:true,
      note:'✅ 正确。关注数是留存的先行指标——关注少了，后续内容消费的"自主召回"能力就弱。7 日留存目前不显著不能作为安全证据（可能只是观察期不够）。' },
    { t:'关注数跌了，说明实验失败，应该直接放弃', ok:false,
      note:'❌ 走另一个极端。时长提升是真实收益，不能因为一个指标下跌就全盘否定——正确做法是判断这个损失能否通过设计优化规避。' },
    { t:'7 日留存不显著说明没有影响，可以忽略关注数的下跌', ok:false,
      note:'❌ "不显著"只说明当前样本没检出差异，不等于"没有影响"。把不显著当"无影响"是常见误读。' }
  ],
  keyPoints:[
    '要区分「短期指标」和「长期机制的先行指标」（如关注数、关系链）',
    '不显著 ≠ 无影响，可能只是观察期或样本不足',
    '指标冲突时的选项不只是"全上/全否"，还有分层设计'
  ],
  takeaway:'当一个改动改善了 A 却损害了 B，先判断**B 是不是 A 的长期基础**。如果时长靠"喂即时满足"换来、却削弱了用户主动建立关系的能力，那是在透支未来。'
},
{
  id:'a09', tag:'业务显著性', icon:'💰',
  scenario:'实验：优化后的下单流程使转化率从 3.00% 提升到 3.12%（相对 +4%），p=0.02。该流程改动需要增加约 3 人日的开发与后续维护成本，且使代码复杂度上升。按当前流量测算，转化率提升带来的年化收入约 +18 万元。',
  ask:'该不该上线？',
  options:[
    { t:'不显著，不上线', ok:false,
      note:'❌ p=0.02 < 0.05 已经显著，事实判断错了。' },
    { t:'收益（年化 +18 万）明显覆盖成本（3 人日 + 维护），且统计显著、无护栏风险 → 建议上线', ok:true,
      note:'✅ 正确。这是"统计显著 + 业务显著 + 成本可覆盖"三者都满足的情形。注意结论要写清测算依据，而不是只说"数据好"。' },
    { t:'提升只有 4%，太小了，不值得做', ok:false,
      note:'❌ "4% 太小"是拍的判断。要算绝对金额：如果基盘大，4% 相对提升可能是数百万——**永远用绝对值算收益，不要用相对数下结论**。' },
    { t:'既然显著就一定上线，不需要看成本', ok:false,
      note:'❌ 忽略了成本。本案例恰好成本可覆盖，但如果年化收益只有 1 万元而维护成本更高，结论就反过来了。' }
  ],
  keyPoints:[
    '决策三要素：统计显著 + 业务显著 + 成本可覆盖',
    '收益要用绝对值（金额）测算，不要用相对百分比下结论',
    '相对提升小 ≠ 不值得做；相对提升大 ≠ 值得做'
  ],
  takeaway:'上线决策的完整句式是：**"这个改动带来 X 元的年化收益，需要 Y 的成本，且护栏指标没问题，所以值得/不值得做"**。缺了任何一段，都只是半个判断。'
}
'''

# ============ cases +2 ============
CASES_NEW = r'''
{
  id:'c06', icon:'📈', tag:'指标背离',
  title:'次日留存涨了，但 7 日留存跌了',
  symptom:'现象：某次版本更新后，新用户次日留存从 31% 提升到 34%（明显改善），但 7 日留存从 19% 掉到 16%。同期新用户渠道结构没有明显变化。\n可用数据：分版本/分人群的留存曲线、功能使用数据、push 发送记录、内容消费数据。',
  deliverable:'① 判断这是好事还是坏事 ② 给出最可能的原因 ③ 给出验证方法与决策建议',
  hypotheses:[
    { h:'用"强提醒"换来的次日回访（最可能）', how:'检查新版本是否增强了 push/签到/红点提醒；看次日回访用户中"被 push 唤起"的比例是否明显上升；对比这些用户的次日使用时长是否下降',
      verdict:'参考结论：如果次日留存涨但"次日使用时长/消费深度"下降，说明回来的用户只是被提醒唤起、点开就走——这类"虚假留存"会在第 7 天暴露（因为用户已经对提醒免疫）。' },
    { h:'新手引导改变了用户预期', how:'看新版本引导是否过度承诺（如"看完 3 个视频领奖励"）；看引导完成率与引导后 7 天内的流失节点',
      verdict:'参考结论：奖励型引导会把"为奖励而来"的用户引进来，奖励拿到后立即流失，表现为 D1 高、D7 低。' },
    { h:'假信号（口径/埋点变化）', how:'检查留存口径是否变了（如 login 事件定义、去重逻辑、时区）；看同时期其他指标是否出现同步的异常跳变',
      verdict:'参考结论：先排除口径变化。版本更新常伴随埋点调整，"指标变化"可能只是统计口径变化。' },
    { h:'用户结构变化（辛普森悖论）', how:'按渠道/城市/机型分层算 D1 与 D7，看是否每个细分人群都呈现"涨 D1 跌 D7"',
      verdict:'参考结论：如果分层后各人群 D7 都没跌，只是整体跌，说明是人群结构问题，不是版本问题。案例中渠道结构没变，所以这个可能性较低。' },
    { h:'产品真正改善了短期体验但伤害了长期价值', how:'检查是否调整了内容分发（如更强调即时刺激的短视频流）；看 7 天内用户的"主动搜索/关注"行为是否下降',
      verdict:'参考结论：这是最需要警惕的情况——短期指标变好靠的是"即时满足"，代价是用户主动建立长期关系的能力下降。' }
  ],
  conclusion:'参考结论：**D1 涨 D7 跌，几乎总是"用短期手段换长期价值"的信号**。最可能是强化提醒/奖励引导带来的虚假留存。判断的关键是看"次日回来的用户在做什么"——如果消费深度、主动关注、搜索行为都在下降，那就是透支。**决策建议：不要因为 D1 涨就保留该改动**，应先去掉强提醒/奖励，看 D1 是否回落到原水平、D7 是否恢复；如果确认是提醒问题，可以改为"更精准的提醒"而不是"更高频的提醒"。',
  actions:[
    '做归因拆分：把次日回访用户按"主动打开 / push 唤起"分类，分别看他们的 D7 留存——这个对比能直接证明是否"虚假留存"',
    '看质量指标而不只是留存数字：次日使用时长、内容消费深度、主动搜索/关注率',
    '如果要保留提醒能力，改为分层策略（只对高意向用户提醒），并设 push 频次上限'
  ],
  pitfalls:[
    '看到 D1 涨就认为版本成功——留存数字必须配合质量指标看',
    '忽略口径变化的可能——版本更新常同时改埋点',
    '不做分层就下结论——要区分"同一批人变了"还是"换了一批人"'
  ],
  takeaway:'留存类指标要成组看：**D1 看兴趣、D7 看习惯、D30 看价值**。只看最近的那个数字，一定会被短期手段骗到。'
},
{
  id:'c07', icon:'🎁', tag:'活动拉新',
  title:'活动拉来的用户质量很差，还要继续做吗？',
  symptom:'现象：一次"邀请好友得会员"活动带来了 3 万新用户（占当月新增的 45%），但活动用户次日留存仅 9%（自然流量 30%），付费率 0.3%（大盘 3%），且活动成本折算下来 CAC 高达 62 元（付费渠道平均 18 元）。运营团队认为"至少量上去了"。\n可用数据：分来源的新增/留存/付费、活动成本明细、优惠券核销与退款数据、用户后续行为。',
  deliverable:'① 判断这次活动是赚还是亏 ② 定位问题出在哪一环 ③ 给出是否继续以及如何改进的建议',
  hypotheses:[
    { h:'激励设计吸引的是"羊毛用户"', how:'看活动用户领奖后的行为——如果领奖即流失、优惠券核销后无复购，说明用户就是为奖励而来；看是否有批量注册特征（同设备/IP 多账号）',
      verdict:'参考结论：这是最可能的原因。"邀请得会员"这类无门槛奖励，会精准吸引到以薅取为目的的用户，他们本来就不是目标人群。' },
    { h:'算账口径不完整（可能没那么亏）', how:'重新核算真实成本：奖励成本 + 履约成本 + 后续服务成本；同时算这些用户带来的收入（不只是首单）',
      verdict:'参考结论：必须算完整的账。但按案例中的数字（CAC 62 元、付费率 0.3%），即使算上长期收入，大概率仍是亏的——要用 LTV/CAC 说话。' },
    { h:'渠道质量差异被平均掩盖', how:'把活动用户按"邀请人类型"拆分——高活跃用户邀请来的人 vs 低活跃用户邀请来的人，质量可能差异巨大',
      verdict:'参考结论：这是关键洞察。不是所有活动用户都差，而是"谁能邀请"没有门槛。如果限定只能由高活跃用户邀请，质量会显著提升。' },
    { h:'归因错误（这批人本来就会来）', how:'做对照：同期没有收到邀请的用户中，有多少是自然注册的；或用历史同期数据对比',
      verdict:'参考结论：如果活动期自然新增没有下降（只是叠加），说明这些确实是增量；但如果自然新增被"替代"了（本来会自然注册的人被邀请链接引导），那就是虚假增量。' }
  ],
  conclusion:'参考结论：**这次活动大概率是亏的**——CAC 62 元 vs 付费渠道平均 18 元，且留存与付费率都远低于大盘，意味着 LTV 也远低于大盘，LTV/CAC 很可能远小于 1。但"量上去了"这个说法本身也站不住：**低质量增量会污染所有后续指标**（拉低整体留存/付费率），还挤占了运营资源。\n\n不过活动形式不必完全放弃，问题出在**激励设计与邀请门槛**：①设置门槛（只有活跃用户可邀请 / 被邀请人需完成关键行为才发奖）②改用双边、延迟发放的奖励 ③把"拉新量"考核改为"高质量拉新量（次留达标才计入）"。',
  actions:[
    '立刻停掉无条件奖励版本，改为"被邀请人完成次日留存后才发奖"——用留存过滤羊毛用户，这是最有效的单点改进',
    '重算完整账：按 LTV/CAC 评估活动，并把结论写成一页复盘（含"量上去了"为什么是伪命题）',
    '改考核口径：把活动的 KPI 从"新增用户数"改为"达到次留标准的新增用户数"或"活动带来的净收入"'
  ],
  pitfalls:[
    '用"量上去了"掩盖质量问题——拉新数量从来不是终点，可留存的量才是',
    '只算奖励成本不算完整成本（履约/服务/后续补贴）',
    '因为算出来亏就完全放弃活动——正确做法是改激励设计，而不是否定整个渠道'
  ],
  takeaway:'活动拉新的第一原则：**奖励要发给"完成关键行为的人"，而不是"完成注册的人"**。用留存或付费作为发奖条件，能自动过滤掉大部分羊毛用户。'
}
'''

append(BASE + r'\lab.js', LAB_NEW, 'lab.js (+4)')
append(BASE + r'\metrics.js', METRICS_NEW, 'metrics.js (+2)')
append(BASE + r'\abtest.js', ABTEST_NEW, 'abtest.js (+3)')
append(BASE + r'\cases.js', CASES_NEW, 'cases.js (+2)')
